import { Typography } from "antd";

export default function AppLogo() {
  return (
    <Typography.Title
      level={4}
      style={{
        color: "#fff",
        margin: 0,
      }}
    >
      FISHERY
    </Typography.Title>
  );
}