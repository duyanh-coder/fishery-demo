import { Typography } from "antd";

export default function AppLogo() {
  return (
    <Typography.Title
      level={4}
      style={{
        color: "#fff",
        margin: 0,
      }}
    >
      HỆ THỐNG THỦY SẢN
    </Typography.Title>
  );
}